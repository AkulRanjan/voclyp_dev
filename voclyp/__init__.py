"""VoClyp — conversation-to-insight platform.

A pipeline of swappable stages wrapped in a platform, organized around three
stable contracts: the gateway (way in), the versioned insight schema (way out),
and taxonomy configs (industry behavior as data, not code).
"""

__version__ = "0.1.0"
