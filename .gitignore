__pycache__/
*.pyc
# runtime data dir at the repo root only — NOT web/src/data (anchored with /)
/data/
*.db
.venv/
/dist/
web/dist/
*.egg-info/
.claude/
node_modules
# secrets — local dev env files (real keys live here; never commit)
.env
.env.*
!.env.example
# Expo local state
.expo/
# Next.js build output + generated types (marketing site)
.next/
next-env.d.ts
*.tsbuildinfo
# build / tooling caches
.pytest_cache/
dist/
# scratch QR / screenshots
expo-qr.png
*.local.png
